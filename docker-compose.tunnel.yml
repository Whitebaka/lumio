services:
  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: photo-proof-cloudflared
    restart: unless-stopped
    command: ["tunnel", "--no-autoupdate", "run"]
    env_file:
      - ../cloudflared/.env
    networks:
      - lumio_net
